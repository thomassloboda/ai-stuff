# Datadog Notes

When Datadog outputs are available:
- note the monitor or dashboard source clearly
- capture relevant timeframe, environment, and service
- correlate trace spikes, error rates, and deployment timing when possible
- avoid over-interpreting a single dashboard without log or trace support
