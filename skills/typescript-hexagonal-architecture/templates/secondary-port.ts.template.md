# Secondary Port TypeScript Template

```ts
export interface {SecondaryPortName} {
    {methodName}({argumentName}: {ArgumentType}): Promise<{ReturnType}>;
}
```

## Notes

- Place under `src/core/ports/secondary`.
- Model a capability, not a concrete technical implementation.
